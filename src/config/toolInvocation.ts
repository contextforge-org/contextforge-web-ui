/** Maximum time the UI waits for a live tool invocation to complete. */
export const TOOL_INVOKE_TIMEOUT_MS = 120_000;

/** Time a live invocation must remain pending before cancellation is offered. */
export const TOOL_CANCEL_REVEAL_DELAY_MS = 2_000;
